# 2026-04-19

## 会话摘要
完成 Feature 007: 系统化移除旧版 Repository 工作流，收敛为单一图谱架构。

## 记录
- [决策] 保留 python-multipart（FastAPI UploadFile 依赖），移除 jinja2
- [决策] python-frontmatter 保留（graph/indexer.py 使用 frontmatter.loads()）
- [决策] __init__.py 的 create_app 改为从 api.app 导入 create_api_app 的别名，保持公共 API 不变
- [经验] 删除 app.py 后 __init__.py 的 re-export 必须同步更新，否则 uv sync 后全面 ImportError
- [经验] test_graph_store 中 legacy schema 拒绝测试用 raw SQL 替代 Repository 实例化，解耦更彻底
- [进展] 删除 15 个纯遗留文件 + 编辑 6 个混合文件 + 重写 ARCHITECTURE.md
- [进展] 基线 72 测试 → 移除 30 个遗留测试 → 最终 42 测试全部通过
- [进展] CLI init/ingest、frontend build、zero legacy grep 均验证通过
- [决策] 影响分析 AI 精判改为按下游文档批量判断候选章节，避免单次请求内按章节 fan-out 多次外呼 LLM
- [经验] 影响分析页的重复 LLM 日志根因在后端 analyzer 的候选章节逐条精判，而不是前端重复提交 analyze 请求
- [进展] 新增 impact analyzer 单元测试，验证多个候选章节只触发一次 LLM 调用；全量测试更新为 43 通过

## Retain
- → bank/experience: python-multipart 是 FastAPI UploadFile 的隐式依赖，不能随 jinja2 一起移除
- → bank/experience: 删文件后立即检查包级 __init__.py 的 re-export，避免级联 ImportError
- → bank/experience: 影响分析的 AI 精判必须按文档批量调用；按候选章节逐条外呼会在真实图谱上指数放大日志与延迟
